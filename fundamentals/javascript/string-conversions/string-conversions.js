
let firstName = "John";
let lastName = "Cokos";
let fullName = firstName.concat(' ', lastName);
fullName;


let string = "Code Fellows";
// string.replace("Code", "John");

// How many ways can lowercase the first char of a string?

// string.trim()[0].toLowerCase();
// string.trim().toLowerCase()[0];
// string.trim().toLowerCase().startsWith('c');
// string.trim().charAt(0).toLowerCase();
// string.trim().replace(/^([A-Z]{1})/, (match) => match.toLowerCase());
// string.trim().match(/^([A-Z]{1})/g)[0].toLowerCase();
